package poc.ide.gen;

import poc.ide.proj.CodeSet;
import poc.ide.proj.FsLocation;

public class CodeAdder
{
	
	public static void addCode(FsLocation location, CodeSet set, Filter filter)
	{
		if (filter == null)
		{
			filter = Filter.ALL;
		}
		
	}

}
